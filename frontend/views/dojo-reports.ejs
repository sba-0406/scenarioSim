<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ScenarioSim | Performance Reports</title>
    <link rel="stylesheet" href="/css/style.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&family=JetBrains+Mono:wght@400;700&display=swap"
        rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --bg-dark: #030712;
            --glass-panel: rgba(17, 24, 39, 0.7);
            --glass-border: rgba(255, 255, 255, 0.08);
            --neon-accent: #6366f1;
            --neon-danger: #f43f5e;
            --neon-success: #10b981;
            --neon-warning: #f59e0b;
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
        }

        body {
            font-family: 'Outfit', sans-serif;
            background: var(--bg-dark);
            color: var(--text-main);
            min-height: 100vh;
        }

        .reports-container {
            max-width: 1000px;
            margin: 100px auto 40px;
            padding: 0 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 50px;
        }

        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            background: linear-gradient(to right, #6366f1, #a855f7);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .header p {
            color: var(--text-muted);
        }

        .report-card {
            background: var(--glass-panel);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 25px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: transform 0.2s ease, border-color 0.2s ease;
            backdrop-filter: blur(10px);
        }

        .report-card:hover {
            transform: translateY(-2px);
            border-color: var(--neon-accent);
        }

        .report-info h3 {
            font-size: 1.3em;
            margin-bottom: 5px;
            color: #fff;
        }

        .report-meta {
            font-size: 0.85em;
            color: var(--text-muted);
            display: flex;
            gap: 15px;
        }

        .grade-pill {
            padding: 8px 20px;
            border-radius: 12px;
            font-weight: 700;
            font-size: 1.5em;
            min-width: 80px;
            text-align: center;
        }

        .grade-S {
            background: rgba(168, 85, 247, 0.2);
            color: #a855f7;
            border: 1px solid rgba(168, 85, 247, 0.3);
        }

        .grade-A {
            background: rgba(16, 185, 129, 0.2);
            color: #10b981;
            border: 1px solid rgba(16, 185, 129, 0.3);
        }

        .grade-B {
            background: rgba(99, 102, 241, 0.2);
            color: #6366f1;
            border: 1px solid rgba(99, 102, 241, 0.3);
        }

        .grade-C {
            background: rgba(245, 158, 11, 0.2);
            color: #f59e0b;
            border: 1px solid rgba(245, 158, 11, 0.3);
        }

        .grade-D {
            background: rgba(244, 63, 94, 0.2);
            color: #f43f5e;
            border: 1px solid rgba(244, 63, 94, 0.3);
        }

        .grade-F {
            background: rgba(100, 116, 139, 0.2);
            color: #64748b;
            border: 1px solid rgba(100, 116, 139, 0.3);
        }

        .view-btn {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--glass-border);
            color: var(--text-main);
            padding: 10px 20px;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .view-btn:hover {
            background: var(--neon-accent);
            border-color: var(--neon-accent);
        }

        .empty-state {
            text-align: center;
            padding: 60px;
            background: var(--glass-panel);
            border: 1px solid var(--glass-border);
            border-radius: 24px;
            color: var(--text-muted);
        }

        /* Modal Styles */
        .modal {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.9);
            z-index: 2000;
            backdrop-filter: blur(10px);
            padding: 40px 20px;
            overflow-y: auto;
        }

        .modal-content {
            background: #1e293b;
            max-width: 700px;
            margin: 0 auto;
            border-radius: 24px;
            padding: 40px;
            position: relative;
            border: 1px solid var(--glass-border);
        }

        .close-modal {
            position: absolute;
            top: 20px;
            right: 20px;
            background: none;
            border: none;
            color: var(--text-muted);
            font-size: 1.5em;
            cursor: pointer;
        }

        .skill-list {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin: 30px 0;
        }

        .skill-item {
            background: rgba(255, 255, 255, 0.03);
            padding: 15px;
            border-radius: 16px;
        }

        .skill-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            font-size: 0.9em;
        }

        .progress-bg {
            height: 6px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 3px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: var(--neon-accent);
            box-shadow: 0 0 10px var(--neon-accent);
        }

        .recommendation {
            background: rgba(99, 102, 241, 0.1);
            padding: 20px;
            border-radius: 16px;
            border: 1px solid rgba(99, 102, 241, 0.2);
            line-height: 1.7;
        }
    </style>
</head>

<body>
    <div class="navbar">
        <div class="container"
            style="display:flex; justify-content:space-between; align-items:center; width:100%; margin:0; max-width:none; padding:0 40px;">
            <h2 style="margin:0;">ScenarioSim</h2>
            <div id="nav-links"></div>
        </div>
    </div>

    <div class="reports-container">
        <div class="header">
            <h1>📊 Performance Dashboard</h1>
            <p>Review your training history and leadership growth</p>
        </div>

        <% if (sessions && sessions.length> 0) { %>
            <div class="reports-list">
                <% sessions.forEach(session=> { %>
                    <div class="report-card">
                        <div class="report-info">
                            <h3>
                                <%= session.archetype.role %> Simulation
                            </h3>
                            <div class="report-meta">
                                <span>📅 <%= new Date(session.completedAt).toLocaleDateString() %></span>
                                <span>🧩 <%= session.scenarioProgress.scenarios.length %> Scenarios</span>
                            </div>
                        </div>
                        <div style="display: flex; align-items: center; gap: 30px;">
                            <div class="grade-pill grade-<%= session.finalReport.overallGrade %>">
                                <%= session.finalReport.overallGrade %>
                            </div>
                            <button class="view-btn" onclick="showReport(allReports['<%= session._id %>'])">
                                View Details
                            </button>
                        </div>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <div class="empty-state">
                    <div style="font-size: 4em; margin-bottom: 20px;">🏜️</div>
                    <h2>No reports yet</h2>
                    <p>Complete a simulation role to see your assessment here.</p>
                    <button onclick="window.location.href='/dojo/roles'" class="view-btn"
                        style="margin-top: 20px; background: var(--neon-accent); border: none;">Start Training</button>
                </div>
                <% } %>
    </div>

    <div id="reportModal" class="modal">
        <div class="modal-content">
            <button class="close-modal" onclick="closeModal()">✕</button>
            <div id="modalBody"></div>
        </div>
    </div>

    <div id="reportsData" style="display: none;">
        <% sessions.forEach(session=> { %>
            <div class="report-data" data-id="<%= session._id %>"
                data-json='<%- JSON.stringify(session.finalReport).split("'").join(" &apos;") %>'></div>
            <% }) %>
    </div>

    <script src="/js/main.js"></script>
    <script>
        const allReports = {};
        document.querySelectorAll('.report-data').forEach(div => {
            allReports[div.dataset.id] = JSON.parse(div.dataset.json);
        });

        function showReport(report) {
            const body = document.getElementById('modalBody');

            let skillsHtml = '';
            if (report.skillScores) {
                Object.entries(report.skillScores).forEach(([skill, score]) => {
                    const percent = Math.min(100, (score / 50) * 100);
                    skillsHtml += `
                        <div class="skill-item">
                            <div class="skill-header">
                                <span>${skill}</span>
                                <span style="color: var(--neon-accent); font-weight: bold;">${score} pts</span>
                            </div>
                            <div class="progress-bg">
                                <div class="progress-fill" style="width: ${percent}%"></div>
                            </div>
                        </div>
                    `;
                });
            }

            body.innerHTML = `
                <span style="font-size: 0.8em; color: var(--neon-accent); text-transform: uppercase; letter-spacing: 2px;">Simulation Assessment: ${report.roleAssessed}</span>
                <h1 style="font-size: 3.5em; margin: 10px 0 25px;">Grade: ${report.overallGrade}</h1>
                
                <h4 style="margin-bottom: 15px; font-size: 1.1em; color: #fff;">Behavioral Competencies</h4>
                <div class="skill-list">${skillsHtml}</div>

                <div class="recommendation">
                    <h4 style="color: var(--neon-accent); margin-bottom: 10px; font-size: 0.9em; text-transform: uppercase;">Expert Summary</h4>
                    <p style="color: #cbd5e1; font-size: 1em;">${report.recommendation}</p>
                </div>

                <div style="margin-top: 30px; display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div style="padding: 15px; background: rgba(16, 185, 129, 0.1); border-radius: 12px; border: 1px solid rgba(16, 185, 129, 0.2);">
                        <span style="display: block; font-size: 0.75em; color: #10b981; text-transform: uppercase;">Top Strengths</span>
                        <p style="margin-top: 5px; font-size: 0.9em;">${report.gapAnalysis.strengths.join(', ') || 'Identifying...'}</p>
                    </div>
                    <div style="padding: 15px; background: rgba(245, 158, 11, 0.1); border-radius: 12px; border: 1px solid rgba(245, 158, 11, 0.2);">
                        <span style="display: block; font-size: 0.75em; color: #f59e0b; text-transform: uppercase;">Growth Areas</span>
                        <p style="margin-top: 5px; font-size: 0.9em;">${report.gapAnalysis.improvements.join(', ') || 'None identified'}</p>
                    </div>
                </div>
            `;

            document.getElementById('reportModal').style.display = 'block';
            document.body.style.overflow = 'hidden';
        }

        function closeModal() {
            document.getElementById('reportModal').style.display = 'none';
            document.body.style.overflow = 'auto';
        }

        // Close on background click
        window.onclick = function (event) {
            if (event.target == document.getElementById('reportModal')) {
                closeModal();
            }
        }
    </script>
</body>

</html>