<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ScenarioSim | Role Selection</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Outfit', sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #f8fafc;
            min-height: 100vh;
        }

        .roles-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 60px;
        }

        .header h1 {
            font-size: 3em;
            margin-bottom: 15px;
            background: linear-gradient(to right, #6366f1, #a855f7);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .header p {
            font-size: 1.2em;
            color: #94a3b8;
        }

        .role-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            margin-bottom: 40px;
        }

        .role-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 40px 30px;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .role-card:hover {
            transform: translateY(-10px);
            border-color: #6366f1;
            box-shadow: 0 20px 40px rgba(99, 102, 241, 0.2);
        }

        .role-icon {
            font-size: 4em;
            margin-bottom: 20px;
        }

        .role-card h2 {
            font-size: 1.8em;
            margin-bottom: 15px;
            color: #fff;
        }

        .role-card p {
            color: #94a3b8;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .scenarios-list {
            text-align: left;
            background: rgba(0, 0, 0, 0.3);
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .scenarios-list h4 {
            color: #6366f1;
            font-size: 0.85em;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 10px;
        }

        .scenarios-list ul {
            list-style: none;
            font-size: 0.9em;
            color: #cbd5e1;
        }

        .scenarios-list li {
            margin-bottom: 5px;
            padding-left: 20px;
            position: relative;
        }

        .scenarios-list li::before {
            content: "→";
            position: absolute;
            left: 0;
            color: #6366f1;
        }

        .enter-btn {
            background: linear-gradient(135deg, #6366f1, #a855f7);
            color: white;
            border: none;
            padding: 14px 30px;
            border-radius: 10px;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }

        .enter-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 20px rgba(99, 102, 241, 0.4);
        }

        .back-btn {
            display: inline-block;
            margin-top: 30px;
            padding: 12px 24px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            color: #94a3b8;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .back-btn:hover {
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
        }

        .grade-badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 6px;
            font-weight: 700;
            font-size: 0.8em;
            margin-left: 5px;
        }

        .grade-S {
            background: #a855f7;
            color: #fff;
            box-shadow: 0 0 10px rgba(168, 85, 247, 0.4);
        }

        .grade-A {
            background: #10b981;
            color: #fff;
            box-shadow: 0 0 10px rgba(16, 185, 129, 0.4);
        }

        .grade-B {
            background: #6366f1;
            color: #fff;
            box-shadow: 0 0 10px rgba(99, 102, 241, 0.4);
        }

        .grade-C {
            background: #f59e0b;
            color: #fff;
            box-shadow: 0 0 10px rgba(245, 158, 11, 0.4);
        }

        .grade-D {
            background: #f43f5e;
            color: #fff;
            box-shadow: 0 0 10px rgba(244, 63, 94, 0.4);
        }

        .grade-F {
            background: #64748b;
            color: #fff;
        }

        .performance-info {
            display: flex;
            flex-direction: column;
            gap: 8px;
            margin-bottom: 20px;
            padding: 12px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 12px;
            text-align: left;
            font-size: 0.85em;
        }

        .perf-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
    </style>
</head>

<body>
    <div class="navbar">
        <div class="container"
            style="display:flex; justify-content:space-between; align-items:center; width:100%; margin:0; max-width:none; padding:0 40px;">
            <h2 style="margin:0;">ScenarioSim</h2>
            <div id="nav-links">
                <!-- Links built via main.js -->
            </div>
        </div>
    </div>

    <div class="roles-container">
        <div class="header">
            <h1>🎬 ScenarioSim</h1>
            <p style="max-width: 700px; margin: 0 auto 15px; line-height: 1.6;">
                Master the art of leadership through immersive, AI-powered simulations.
                Experience realistic high-stakes workplace challenges, test your judgment in a safe environment,
                and receive immediate expert feedback to refine your management style.
            </p>
            <p
                style="color: var(--neon-accent); font-weight: 600; text-transform: uppercase; letter-spacing: 1px; font-size: 0.9em;">
                Choose your role and face 3 challenging scenarios
            </p>
        </div>

        <div class="role-grid">
            <% const roles=[ { id: `Manager`, icon: `👔`, desc: `Lead teams through crisis, performance issues, and
                tough decisions`, scenarios: [`Angry Client Escalation`, `Underperforming Team`, `Budget Crisis`] }, {
                id: `Developer`, icon: `💻`, desc: `Navigate technical disputes, scope creep, and mentorship
                challenges`, scenarios: [`Technical Disagreement`, `Scope Pressure`, `Struggling Junior Dev`] }, { id:
                `HR`, icon: `👥`, desc: `Handle harassment claims, salary negotiations, and team conflicts`, scenarios:
                [`Harassment Complaint`, `Salary Negotiation`, `Team Conflict`] }, { id: `Executive`, icon: `🎯`, desc:
                `Manage board challenges, major escalations, and high-stakes negotiations`, scenarios: [`Board Strategy
                Challenge`, `Client CEO Escalation`, `Union Negotiation`] } ]; %>

                <% roles.forEach(role=> { %>
                    <div class="role-card" onclick="enterSimulation('<%= role.id %>')">
                        <div class="role-icon">
                            <%= role.icon %>
                        </div>
                        <h2>
                            <%= role.id %>
                        </h2>
                        <p>
                            <%= role.desc %>
                        </p>

                        <% if (typeof stats !=='undefined' && stats && stats[role.id]) { %>
                            <div class="performance-info">
                                <div class="perf-row">
                                    <span style="color: #94a3b8;">Recent Grade</span>
                                    <span class="grade-badge grade-<%= stats[role.id].recent %>">
                                        <%= stats[role.id].recent %>
                                    </span>
                                </div>
                                <div class="perf-row">
                                    <span style="color: #94a3b8;">Best Grade</span>
                                    <span class="grade-badge grade-<%= stats[role.id].best %>">
                                        <%= stats[role.id].best %>
                                    </span>
                                </div>
                            </div>
                            <% } %>

                                <div class="scenarios-list">
                                    <h4>3 Scenarios</h4>
                                    <ul>
                                        <% role.scenarios.forEach(s=> { %>
                                            <li>
                                                <%= s %>
                                            </li>
                                            <% }) %>
                                    </ul>
                                </div>
                                <button class="enter-btn">Enter Simulation</button>
                    </div>
                    <% }) %>
        </div>

        <div style="text-align: center;">
            <a href="/dojo/roles" class="back-btn">← Refresh Roles</a>
        </div>
    </div>

    <script src="/js/main.js"></script>
    <script>
        async function enterSimulation(role) {
            try {
                const res = await fetch('/dojo/start', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ role }),
                    credentials: 'include'
                });

                const data = await res.json();
                if (data.success) {
                    window.location.href = `/dojo/simulation/${data.data._id}`;
                } else {
                    alert('Failed to start simulation');
                }
            } catch (err) {
                console.error(err);
                alert('Error starting simulation');
            }
        }
    </script>
</body>

</html>